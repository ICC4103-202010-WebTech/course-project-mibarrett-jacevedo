<%= form_with(model: event_vote, local: true) do |form| %>
  <% if event_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_vote.errors.count, "error") %> prohibited this event_vote from being saved:</h2>

      <ul>
        <% event_vote.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
