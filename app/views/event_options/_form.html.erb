<%= form_with(model: event_option, local: true) do |form| %>
  <% if event_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_option.errors.count, "error") %> prohibited this event_option from being saved:</h2>

      <ul>
        <% event_option.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
