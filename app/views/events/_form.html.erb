<div class="row ml-0">
  <%= form_with model: event, local: true do |form| %>
    <div class="form-group">
      <%= form.label :title %>
      <%= form.text_field :title, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= form.label :description %>
      <%= form.text_area :description, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= form.label :location %>
      <%= form.text_field :location, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= form.label :starting_vote_day %>
      <%= form.datetime_local_field :starting_vote_day %>
    </div>

    <div class="form-group mt-2">
      <%= form.submit submit_btn_text, class: ["btn", "btn-primary"] %>
      <%= link_to "Back", :back, class: ["btn", "btn-secondary"] %>
    </div>

    <div class="card">
      <div class="card-body">
        <h5 class="card-title"><%= event_option_title %></h5>
        <p><%= event_option_instruction_text %></p>
        <%=form.fields_for :event_options do |event_options_form| %>
          <div class="form-group">
            <%= event_options_form.label :day %>
            <%= event_options_form.datetime_local_field :day %>

            <% if action_name == 'edit' %>
              <%= event_options_form.label "Delete"%>
              <%= event_options_form.check_box :_destroy %>
            <% end %>
            <hr>
          </div>
        <% end %>
      </div>
    </div>

  <% end %>
</div>