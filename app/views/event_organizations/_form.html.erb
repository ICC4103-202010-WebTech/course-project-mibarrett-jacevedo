<%= form_with(model: event_organization, local: true) do |form| %>
  <% if event_organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_organization.errors.count, "error") %> prohibited this event_organization from being saved:</h2>

      <ul>
        <% event_organization.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
